# Perp Intel (Binance USD-M) — Funding / OI / Basis

A quantitative “intel terminal” for perpetual futures:
- Funding rate + regimes + crowding score
- Open interest history + impulses
- Basis history (PERPETUAL)
- Live markPrice + funding + liquidation tape

## Setup
```bash
cp .env.example .env
poetry install
